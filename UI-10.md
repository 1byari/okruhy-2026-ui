# Lineární regrese – metoda nejmenších čtverců, geometrická interpretace, problém kolinearity

## 1. Model lineární regrese

V modelu lineární regrese předpokládáme **lineární závislost** vysvětlované proměnné $y$ na hodnotách příznaků:
$$y = w_0 + w_1 x_1 + w_2 x_2 + \dots + w_p x_p + \varepsilon$$

* $w_0$ – **intercept** (absolutní člen).
* $w_1, \dots, w_p$ – neznámé koeficienty (váhy), které hledáme.
* $\varepsilon$ – část $y$, která je nevysvětlitelná pomocí hodnot příznaků (náhodný šum).
* **Důležitý předpoklad:** $\mathbb{E}\varepsilon = 0$.

### Zkrácený zápis
Zavedeme:
* $x = (x_0, x_1, \dots, x_p)^T$ – vektor příznaků, kde $x_0 = 1$.
* $w = (w_0, w_1, \dots, w_p)^T$ – vektor vah.

Model pak zapíšeme jako:
$$y = w^T x + \varepsilon$$

**Predikci $\hat{y}$ definujeme jako:**
$$\hat{y} = \hat{w}^T x = \hat{w}_0 + \hat{w}_1 x_1 + \dots + \hat{w}_p x_p$$

Protože $\mathbb{E}\varepsilon = 0 \implies \mathbb{E}y = w^T x$. Predikce $\hat{y}$ je tedy bodový odhad očekávané hodnoty $\mathbb{E}y$ v bodě $x$.

---

## 2. Metoda nejmenších čtverců (OLS)

Hledáme takovou hodnotu $w$, aby chyba modelu byla co nejmenší (tuto hodnotu pak použijeme jako odhad $\hat{w}$).

K měření chyby využíváme **ztrátovou funkci** $L : \mathbb{R}^2 \to \mathbb{R}$. Obvyklá volba:
$$L(y, \hat{y}) = (y - \hat{y})^2$$

**Residual Sum of Squares (RSS):**
$$RSS(w) = \sum_{i=1}^N L(y_i, w^T x_i) = \sum_{i=1}^N (y_i - w^T x_i)^2$$

Minimalizací RSS získáme odhad $\hat{w}_{OLS}$.

### Maticový zápis
Přepíšeme model pro trénovací množinu s $N$ páry $(y_i, x_i)$:
* $y = (y_1, \dots, y_N)^T \in \mathbb{R}^{N,1}$ – vektor cílů.
* $\varepsilon = (\varepsilon_1, \dots, \varepsilon_N)^T \in \mathbb{R}^{N,1}$ – vektor šumu, $\mathbb{E}\varepsilon = 0$.
* **Matice plánu** $X \in \mathbb{R}^{N, p+1}$ (první sloupec jsou jedničky pro intercept):
  $$X = \begin{pmatrix} x_1^T \\ \vdots \\ x_N^T \end{pmatrix} = \begin{pmatrix} 1 & x_{1,1} & \dots & x_{1,p} \\ \vdots & \vdots & \ddots & \vdots \\ 1 & x_{N,1} & \dots & x_{N,p} \end{pmatrix}$$

Celkový model zapíšeme jako:
$$y = X w + \varepsilon$$

### Minimalizace RSS
$$RSS(w) = \sum_{i=1}^N (y_i - w^T x_i)^2 = \| y - X w \|^2$$

Spočítáme gradient:
$$\nabla RSS = -\sum_{i=1}^N 2 (y_i - w^T x_i) x_i = -2 X^T (y - X w)$$

Položíme gradient roven nule:
$$\nabla RSS = 0 \iff X^T y - X^T X w = 0$$

Získáme tzv. **normální rovnici**:
$$X^T X w = X^T y$$

**Ověření minima (Hessova matice):**
* Hessian $H = 2 X^T X$ je vždy pozitivně semidefinitní (PSD) $\implies$ neostré lokální minimum nastává v jakémkoliv bodě $w$, který řeší normální rovnici.
* Předpokládáme-li, že $X^T X$ je regulární, existuje jednoznačné řešení normální rovnice:
  $$\hat{w}_{OLS} = (X^T X)^{-1} X^T y$$

> **Poznámka:** Pokud je $X^T X$ regulární, je Hessova matice dokonce pozitivně definitní (PD) $\implies$ $\hat{w}_{OLS}$ je bod ostrého lokálního (a tedy i globálního) minima.

**Predikce v novém bodě $x$:**
$$\hat{y} = \hat{w}_{OLS}^T x = x^T \hat{w}_{OLS} = x^T (X^T X)^{-1} X^T y$$

---

## 3. Geometrická interpretace

* Předpokládejme, že sloupce matice $X$ jsou lineárně nezávislé $\implies$ tvoří podprostor.
* Tento podprostor je dán jako $\{ X w \mid w \in \mathbb{R}^{p+1} \} = \langle X_{:0}, \dots, X_{:p} \rangle$.
* Geometricky si jej lze představit jako nadrovinu.
* Predikce $\hat{y} = X w$ je pak **ortogonální projekce** vektoru skutečných hodnot $y$ do této nadroviny (hledáme nejbližší bod v nadrovině k bodu $y$).

**Odvození normální rovnice z geometrie:**
* Hledáme takový vektor $w$, že chybový vektor $(y - X w)$ je kolmý na všechny sloupce matice $X$.
* To znamená: $\forall i \in \widehat{p+1} : (X_{:i})^T (y - X w) = 0$.
* Maticově: $X^T (y - X w) = 0 \iff X^T X w = X^T y$ – stejná normální rovnice, jakou jsme získali pomocí gradientu.

**Motivace při hledání $w$:**
1. Nejlepší shoda s trénovacími daty $\implies$ minimalizujeme vzdálenost $\| y - X w \|$.
2. Minimalizace overfittingu (přeučení) $\implies$ snaha o co nejmenší normu vektoru vah $\| w \|$.

---

## 4. Problém linearity a kolinearity

Problém nastává, když jsou sloupce matice $X$ lineárně závislé (nebo velmi blízko k závislosti). Potom je matice $X^T X$ singulární, nebo numericky nestabilní, což vede k přeučení modelu.

### Problém linearity (přesná lineární závislost)
* Sloupce jsou lineárně závislé $\implies$ neexistuje jednoznačné řešení (matice $X^T X$ není regulární).
* Podprostor $\langle X_{:0}, \dots, X_{:p} \rangle$ má menší dimenzi, než bychom potřebovali.
* **Řešení:** Využívá se **Moorova-Penroseova pseudoinverze** $(X^T X)^+$. Algoritmus zvolí koeficienty u lineárně závislých sloupců tak, aby minimalizoval normu vektoru $w$.

### Problém kolinearity (téměř lineární závislost)
* Sloupce jsou skoro lineárně závislé (např. kvůli silně korelovaným příznakům).
* Standardní metoda (která neminimalizuje normu $\| w \|$) selhává.
* Matice $(X^T X)^{-1}$ sice formálně existuje, ale je výpočetně extrémně nestabilní.
* Koeficient pro některý ze závislých sloupců může „ustřelit" do obrovských hodnot $\implies$ zásadní problém pro model (masivní overfitting).
* **Řešení:** **Regularizace** (např. **Ridge**, **Lasso**), která uměle omezuje velikost vah přidáním penalty za $\| w \|$.
