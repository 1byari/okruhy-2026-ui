# Hřebenová regrese, očekávaná chyba modelu a její rozklad

## 1. Hřebenová regrese (Ridge Regression)

Hřebenová regrese pomáhá vyřešit **problém kolinearity** (kdy je matice $X^T X$ blízko singularitě) zavedením **penalizačního členu** (L2 regularizace).

Upravená chybová funkce:
$$RSS_\lambda(w) = \| y - X w \|^2 + \lambda \sum_{i=1}^p w_i^2$$

* $\lambda \ge 0$ je hyperparametr regulace.
* **$\lambda = 0$:** Obyčejná metoda nejmenších čtverců (OLS).
* **$\lambda > 0$:** Cílíme na vektory vah $w$, které mají co nejmenší složky. Zabraňujeme tak „ustřelení" vah do extrémních hodnot.

### Minimalizace $RSS_\lambda$
Zavedeme upravenou jednotkovou matici $\mathbb{I}' \in \mathbb{R}^{p+1, p+1}$, která má na pozici $(0,0)$ nulu (intercept $w_0$ se obvykle nepenalizuje):
$$\mathbb{I}' = \begin{pmatrix} 0 & 0 & \dots & 0 \\ 0 & 1 & \dots & 0 \\ \vdots & \vdots & \ddots & \vdots \\ 0 & 0 & \dots & 1 \end{pmatrix}$$

Maticový zápis:
$$RSS_\lambda(w) = \| y - X w \|^2 + \lambda w^T \mathbb{I}' w$$

Spočítáme gradient a položíme roven nule:
$$\nabla RSS_\lambda(w) = -2 X^T (y - X w) + 2 \lambda \mathbb{I}' w = 0$$

Získáme **upravenou normální rovnici**:
$$X^T y - X^T X w - \lambda \mathbb{I}' w = 0 \implies (X^T X + \lambda \mathbb{I}') w = X^T y$$

**Ověření minima:**
Hessova matice $H_{RSS_\lambda}(w) = 2 X^T X + 2 \lambda \mathbb{I}'$ je vždy pozitivně definitní (PD). Existuje tedy jednoznačné řešení (globální minimum):
$$\hat{w}_\lambda = (X^T X + \lambda \mathbb{I}')^{-1} X^T y$$

**Predikce v novém bodě $x$:**
$$\hat{y} = x^T \hat{w}_\lambda$$

### Praktické poznámky
* Hledáme optimální hodnotu $\lambda$, pro kterou je střední kvadratická chyba ($MSE = \frac{1}{N} \sum (y_i - x_i^T \hat{w}_\lambda)^2$) na validačních datech co nejmenší.
* Při použití hřebenové regrese je **nutné** jednotlivé příznaky nejdříve **standardizovat**, aby se staly rozsahově srovnatelné (jinak by penalizace nespravedlivě dopadala na příznaky s menšími absolutními hodnotami).

---

## 2. Statistické vlastnosti modelu a rozklad chyby

V modelu předpokládáme $y = X w + \varepsilon$, kde $\varepsilon$ je náhodný chybový vektor ($\mathbb{E}\varepsilon = 0$). Z toho plyne, že i cílová proměnná $y$ je náhodný vektor a naše odhadnuté váhy $\hat{w}_\lambda$ (a tedy i predikce $\hat{y}$) jsou náhodné vektory.

Předpokládáme také **nezávislost trénovacích a testovacích dat** $\implies$ nezávislost predikce $\hat{y}$ a skutečné hodnoty $y$.

### Rozklad očekávané chyby (Expected Error Decomposition)
Střední hodnotu ztrátové funkce $\mathbb{E}L(y, \hat{y})$ pro kvadratickou ztrátu můžeme rozložit následovně:
$$\mathbb{E}L(y, \hat{y}) = \mathbb{E}(y - \hat{y})^2 = \mathbb{E}\big((y - \mathbb{E}y) - (\hat{y} - \mathbb{E}y)\big)^2$$
$$= \mathbb{E}(y - \mathbb{E}y)^2 - 2\,\mathbb{E}\big((y - \mathbb{E}y)(\hat{y} - \mathbb{E}y)\big) + \mathbb{E}(\hat{y} - \mathbb{E}y)^2$$

*Díky nezávislosti je prostřední člen roven nule.* Zbývá:
$$\mathbb{E}L(y, \hat{y}) = \mathbb{E}(y - \mathbb{E}y)^2 + \mathbb{E}(\hat{y} - \mathbb{E}y)^2$$

1. **První část** je rozptyl samotných dat (tzv. **Bayes error** nebo ireducibilní chyba, daná šumem $\varepsilon$):
   $$\mathbb{E}(y - \mathbb{E}y)^2 = \text{var}\, y = \text{var}\, \varepsilon = \sigma^2$$
2. **Druhá část** je $MSE(\hat{y})$, kterou dále rozložíme přičtením a odečtením $\mathbb{E}\hat{y}$:
   $$\mathbb{E}(\hat{y} - \mathbb{E}y)^2 = \mathbb{E}\big((\hat{y} - \mathbb{E}\hat{y}) + (\mathbb{E}\hat{y} - \mathbb{E}y)\big)^2$$
   $$= \mathbb{E}(\hat{y} - \mathbb{E}\hat{y})^2 + (\mathbb{E}\hat{y} - \mathbb{E}y)^2 = \text{var}\, \hat{y} + (\text{bias}\, \hat{y})^2$$

Dohromady dostáváme rozklad očekávané chyby na **3 složky**:
$$\boxed{\,\mathbb{E}L(y, \hat{y}) = \sigma^2 + (\text{bias}\, \hat{y})^2 + \text{var}\, \hat{y}\,}$$
*(Ireducibilní chyba + Vychýlení na druhou + Rozptyl.)*

### Vztah vychýlení a rozptylu (Bias-Variance Tradeoff) pro Ridge regresi
Změnou parametru $\lambda$ ovlivňujeme poměr mezi vychýlením a rozptylem:
* $(\text{bias}\, \hat{y})^2 \sim \left(1 - \frac{1}{1+\lambda}\right)^2$
* $\text{var}\, \hat{y} \sim \left(\frac{1}{1+\lambda}\right)^2$

| Volba $\lambda$    | Bias    | Variance | Chování modelu                |
| ------------------ | :-----: | :------: | ----------------------------- |
| **Větší $\lambda$** | velký   | malý     | *podučený* (underfitting)     |
| **Menší $\lambda$** | malý    | velký    | *přeučený* (overfitting)      |

Tedy s rostoucí $\lambda$ klesá rozptyl a roste vychýlení. Hledáme optimální $\lambda$, pro které bude celková očekávaná chyba co nejmenší.

---

## 3. Nestrannost metody nejmenších čtverců (OLS)

❗️ **Věta:** Odhad $\hat{w}_{OLS}$ je za předpokladu $\mathbb{E}\varepsilon = 0$ **nestranný**, tzn. $\mathbb{E}\hat{w}_{OLS} = w$.

**Důkaz:**
1. Střední hodnota $y$ (využití linearity střední hodnoty):
   $$\mathbb{E}y = \mathbb{E}(X w + \varepsilon) = X w + \mathbb{E}\varepsilon = X w$$
2. Střední hodnota odhadnutých vah:
   $$\mathbb{E}\hat{w}_{OLS} = \mathbb{E}\big[(X^T X)^{-1} X^T y\big] = (X^T X)^{-1} X^T \mathbb{E}y = (X^T X)^{-1} X^T X w = w$$
3. Pro predikci $\hat{y}$:
   $$\mathbb{E}\hat{y} = \mathbb{E}[x^T \hat{w}_{OLS}] = x^T \mathbb{E}\hat{w}_{OLS} = x^T w = \mathbb{E}y$$

Z toho vyplývá, že $\text{bias}\, \hat{y} = \mathbb{E}\hat{y} - \mathbb{E}y = 0$. Vychýlení je nulové, a proto je obyčejná metoda nejmenších čtverců nestranným odhadem.

*(Pozn.: Hřebenová regrese s $\lambda > 0$ je naopak odhadem **vychýleným** – výměnou za snížení rozptylu obětujeme nestrannost.)*
