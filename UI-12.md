# Logistická regrese – sestavení modelu pro binární klasifikaci, trénování jako MLE odhad

## 1. Sestavení modelu pro binární klasifikaci

Logistická regrese je metoda určená primárně pro klasifikaci (omezíme se na **binární klasifikaci**, kde cílová proměnná $y$ může nabývat pouze hodnot z množiny $\{0, 1\}$).

Z diskrétní úlohy klasifikace uděláme spojitý problém:
* Namísto přímého predikování $y \in \{0, 1\}$ se snažíme predikovat **pravděpodobnost**, že bod patří do třídy 1: $P(y=1)$.
* Výsledek tedy musí ležet v intervalu $\langle 0, 1 \rangle$.
* Hledáme funkční předpis, který pro dané hodnoty příznaků $x_i$ a váhy $w$ vrátí číslo z tohoto intervalu.

K tomu využíváme **logistickou funkci (Sigmoidu)**:
$$f(z) = \frac{e^z}{1+e^z} = \frac{1}{1+e^{-z}}$$
* Z dat spočítáme lineární kombinaci $w^Tx$ (kde $x$ je vektor příznaků a $w$ vektor koeficientů/vah).
* Tuto hodnotu dosadíme do sigmoidy, získáme tak predikci $S(w^Tx)$.
* Hodnota $\frac{1}{2}$ je rozhodovací hranice.

---

## 2. Hranice rozhodnutí (Decision Boundary)

Každý datový bod $x$ je bodem v prostoru $\mathbb{R}^p$ (máme $p$ příznaků).
Pro pravděpodobnost, že bod spadá do třídy 1 při pevně zvolených parametrech $w$, platí:
$$P(y=1 | x, w) = \frac{e^{w^Tx}}{1+e^{w^Tx}}$$

* **Klasifikační pravidlo:** Pokud je pravděpodobnost $> 0.5 \implies y=1$, jinak $y=0$.
* Kdy je pravděpodobnost přesně $0.5$? Když platí $w^Tx = 0$.

Tato rovnice tvoří **nadrovinu**:
$$w^Tx = w_0x_0 + w_1x_1 + \dots + w_px_p = 0$$
Tato nadrovina (což je lineární varieta dimenze $p-1$ v prostoru $\mathbb{R}^p$) je vlastně samotnou **rozhodovací hranicí** rozumného klasifikátoru.

---

## 3. Trénování jako MLE odhad (Maximum Likelihood Estimation)

Pro úsporu místa si zavedeme značení:
* $p_1(x, w) = P(y=1 | x, w) = \frac{e^{w^Tx}}{1+e^{w^Tx}}$
* $p_0(x, w) = P(y=0 | x, w) = 1 - \frac{e^{w^Tx}}{1+e^{w^Tx}}$

Pro $i$-tý datový bod v trénovacích datech je pravděpodobnost, že model trefí jeho skutečnou třídu $y_i$, rovna $p_{y_i}(x_i, w)$.

Máme $N$ bodů v trénovacích datech, zapsáno maticově:
$$y = \begin{pmatrix} y_1 \\ \vdots \\ y_N \end{pmatrix} \in \mathbb{R}^N, \quad X = \begin{pmatrix} x_1^T \\ \vdots \\ x_N^T \end{pmatrix} \in \mathbb{R}^{N, p+1}$$

Chceme najít pravděpodobnost konkrétních trénovacích dat při daných parametrech $w$. Předpokládáme, že jednotlivé datové body jsou navzájem nezávislé. 
Věrohodnost (Likelihood) tedy můžeme zapsat jako součin pravděpodobností:
$$L(w) = \prod_{i=1}^N p_{y_i}(x_i, w)$$

Abychom součin převedli na jednodušší součet, aplikujeme logaritmus a hledáme maximum **Log-Likelihood funkce $\ell(w)$**:
$$\ell(w) = \ln L(w) = \sum_{i=1}^N \ln p_{y_i}(x_i, w)$$
Tento zápis můžeme pro binární $y_i$ šikovně rozepsat:
$$\ell(w) = \sum_{i=1}^N \left( y_i \ln p_1(x_i, w) + (1 - y_i) \ln p_0(x_i, w) \right)$$
Po dosazení vzorců pro sigmoidu a algebraických úpravách dostaneme kompaktní tvar:
$$\ell(w) = \sum_{i=1}^N \left( y_i w^Tx_i - \ln(1+e^{w^Tx_i}) \right)$$

---

## 4. Hledání maxima a numerické řešení

Hledáme maximum funkce $\ell(w)$ vzhledem k parametrům $w$. K tomu potřebujeme spočítat derivaci (gradient):
$$\nabla \ell(w) = X^T(y - \mathbb{P})$$
Kde $\mathbb{P}$ je vektor pravděpodobností pro všechny body: $\mathbb{P} = (p_1(x_1, w), \dots, p_1(x_N, w))^T$.

Položením gradientu rovno nule bychom získali normální rovnici. 
**Zásadní problém:** Na rozdíl od lineární regrese zde **neumíme najít explicitní analytické řešení** normální rovnice.

**Řešení:** Musíme použít **numerické aproximativní metody** k iterativnímu hledání maxima.
* Příklady: Gradientní vzestup (Gradient Ascent), Newtonovy metody.
* V obou případech se konstruuje posloupnost vah $w_0, w_1, w_2, \dots$, o které se předpokládá, že konverguje k lokálnímu maximu (díky vlastnostem logistické funkce je zde lokální maximum zároveň globálním).